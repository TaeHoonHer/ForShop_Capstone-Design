<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.pytorch.demo.objectdetection">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_logo_foreground"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <!-- 앞의 코드는 여기에 들어간다 -->

        <activity android:name=".MainActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MyPageActivity"
            android:label="My Page"/>

        <activity
            android:name=".MyBoardActivity"
            android:label="My Gallery"/>

        <activity
            android:name=".LoginActivity"
            android:label="Login"/>

        <activity
            android:name=".SignupActivity"
            android:label="Signup Activity"/>

        <activity
            android:name=".ProfileActivity"
            android:label="Profile Activity"/>

        <activity
            android:name=".UploadActivity"
            android:label="Upload"/>

        <activity
            android:name=".Profile_saved"
            android:label="Profile Saved" >
        </activity>

        <activity
            android:name=".Profile_4shop"
            android:label="Profile 4shop" >
        </activity>

        <activity android:name=".SearchActivity"
            android:label="SearchActivity">
        </activity>

        <activity android:name=".MyBoardActivity2"
            android:label="MyBoardActivity2">
        </activity>


    </application>

</manifest>